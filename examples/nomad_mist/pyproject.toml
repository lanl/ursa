[project]
name = "ursa-example-nomad-mist"
version = "0.1.0"
description = "URSA, Nomad, and MIST MCP integration example"
requires-python = ">=3.11"
dependencies = [
    "ursa-ai",
]

[tool.uv]
package = false

[tool.uv.sources]
ursa-ai = { path = "../..", editable = true }
