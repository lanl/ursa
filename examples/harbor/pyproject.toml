[project]
name = "ursa-example-harbor"
version = "0.1.0"
description = "Run URSA agents with Harbor."
requires-python = ">=3.12"
dependencies = ["ursa-ai[harbor]"]

[tool.uv]
package = false

[tool.uv.sources]
ursa-ai = { path = "../..", editable = true }
