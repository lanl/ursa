Install the CPU build of PyTorch in this Red Hat UBI 10 Minimal environment.
The installation must be available to the system `python3`; importing `torch`
and creating a tensor must both succeed. GPU support is not required.
