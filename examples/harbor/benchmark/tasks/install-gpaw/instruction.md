Install the GPAW electronic-structure package in this Red Hat UBI 10 Minimal
environment. The installation must be available to the system `python3` and
`python3 -c 'import gpaw'` must succeed. You may install any required operating
system or Python dependencies. Do not replace the base operating system.
